# 포트폴리오 v3 구조 재설계

**작성일:** 2026-08-25
**대상 산출물:** `portfolio/heewung-song-portfolio-v3.html` (신규)
**선행 문서:** `docs/plans/2026-08-24-general-portfolio-narrative-design.md`
**사실 근거:** `resume/product-engineer.md`, 각 프로젝트 OpenWiki

## 문제 정의

v2는 다섯 챕터가 모두 같은 틀을 쓴다. `Background → Process → Outcome → My Role` + `Problem State → Designed System`.

이 틀은 이력서 문법이다. "내가 무엇을 했는가"에는 답하지만 "이 시스템이 무엇인가"에는 답하지 않는다. 네 가지 결과가 나온다.

1. **시스템 정의가 없다.** CH02 인바운드는 첫 문장이 "운영시간·DTMF·예약·병원 정보·상담원 연결이 한 통화에 섞입니다"다. 이미 시스템을 아는 사람의 언어이며, 환자가 전화를 걸었을 때 무슨 일이 벌어지는지를 네 장 내내 보여주지 않는다. CH01이 무사한 것은 "직원이 전화를 건다"가 설명 없이도 그려지기 때문이지 틀이 좋아서가 아니다.
2. **결론만 있고 사건이 없다.** "네 가지 안전 불변식", "ZSET/HASH/STREAM 역할 분리"는 판단의 결론이지 근거가 아니다. 독자는 왜 그렇게까지 해야 했는지 모른다.
3. **종속 관계가 보이지 않는다.** CH04 통화 관측·평가는 CH01/CH02가 만든 통화를 분석하는 층인데, 다섯 챕터가 평평하게 나열돼 독립 프로젝트로 읽힌다.
4. **분량과 내용량이 반대다.** CH01은 7장, CH02는 4장이다. CH02가 담은 내용이 더 적지 않다.

## 중심 원칙

### 1. 그림이 먼저, 기술이 다음

시스템 설명은 문단이 아니라 경계도 한 장과 정의 세 줄로 끝낸다. 독자가 머릿속에 상자 하나를 그린 뒤에야 그 안의 부품을 설명한다.

### 2. 기능마다 사건이 붙는다

기능을 나열하지 않는다. 기능 하나를 `무엇을 하는가 → 어떻게 만들었나 → 뭐가 깨졌나` 세 겹으로 판다. **세 번째 겹이 없는 기능은 슬라이드로 만들지 않는다.** 이 규칙이 기능 소개서로 떨어지는 것을 막는다.

세 번째 겹은 **실제 실패 사례**이거나 **의도적으로 그은 경계**다. 후자는 "할 수 있지만 하지 않기로 한 것"이며, 그렇게 정한 이유가 함께 있어야 한다. 어느 쪽이든 근거 문서에서 나와야 하고 각색하지 않는다.

### 3. 수치는 뒤에, 한계는 명시한다

수치를 앞에 두면 그냥 숫자지만 뒤에 두면 판단들을 거쳐 나온 결과가 된다. 마지막에 못 하는 것을 직접 쓴다. 경계를 스스로 긋는 신호가 수치보다 강하다.

## 정보 구조

챕터 넷, 층 하나. 실제 종속 관계가 있을 때만 묶는다.

```
오프닝 · 나는 어떤 문제를 푸는 사람인가
챕터 지도

1 · 병원 전화 Voice AI     거는 전화 + 받는 전화
2 · 통화 관측과 평가       1의 결과를 받는 층
3 · 챗봇 고객상담
4 · AIU 사내 업무지원

클로징 · 반복되는 방식
```

**인바운드와 아웃바운드를 한 챕터로 합친다.** 두 위키를 대조한 결과 기능과 런타임이 대부분 같다.

| 공통 | 근거 |
|---|---|
| `SingleAgent` + `flow_config` 5종 노드 | 양쪽 위키가 동일한 `condition`/`greeting`/`agent`/`action`/`exit` 구성을 기술 |
| DTMF 액션 매핑 5종 | `booking_create` / `booking_inquiry` / `booking_modify` / `booking_cancel` / `info_look_up` |
| `action_mode_handler` 3종 | `auto` / `transfer` / `leave_memo` |
| `GreetingDtmfTask` | 5초 타임아웃, 무효 2회, replay 키, 세션 presence task 억제 |
| **Warm Transfer 큐 공유** | 아웃바운드 위키가 명시한다. "shares a company FIFO queue between inbound/outbound calls" |
| ZSET/HASH/Stream 구조 | 동일한 키 구성과 660초 TTL, 120/240/360초 대기 재확인 |

따로 설명하면 같은 내용을 두 번 쓰게 된다. v2가 실제로 그랬다.

**관측과 평가는 별도 챕터다.** 두 서비스의 분석 파이프라인이 완전히 분리돼 있음을 위키에서 확인했다.

| | 아웃바운드 | 인바운드 |
|---|---|---|
| 입력 토픽 | `aiu.outbound-call.analysis` | `aiu.inbound-call.analysis` |
| 출력 토픽 | `aiu.outbound.agent-call-analysis-complete` | `aiu.inbound.call-end` |
| 테이블 | `conversation_results`, `conversation_events` | `inbound_conversation_results`, `inbound_call_analytics`, `inbound_call_attempts`, `inbound_call_semantics` |
| 판정 방식 | LLM 분류(Trustcall) 중심, 결정론적 classifier와 shallow-merge | 결정론적 `events → attempts → rollup`, 의미 분석은 격리된 별도 층 |

챕터 1의 결과를 받는 후처리이므로 순서상 바로 뒤에 놓지만, 다룰 판단이 독립적이므로 하위 항목이 아닌 챕터로 둔다.

**단, 두 파이프라인을 비교하는 구성으로 쓰지 않는다.** 인바운드 쪽이 결정론적 귀속·의미 분리·버전 관리·백필까지 갖춰 더 발전한 것은 사실이지만, 그것은 두 위키를 대조해 얻은 해석이지 대화분석 시스템 자체의 기능이 아니다. 챕터 2는 **어떤 과정으로 이루어지고 그걸로 무엇을 하는가**로 전개하며, 성숙도 차이는 필요한 곳에서 한 줄로만 언급한다.

**챗봇 고객상담은 하위가 아니다.** `2025.03 – 2026.03`, 채널이 웹·Voice·Kakao인 멀티채널 상담 제품으로 인/아웃바운드와 병렬 진행됐다. 다만 초기 그래프와 서비스 경계를 만들고 팀에 넘긴 프로젝트이므로 짧게 다룬다.

챕터 번호는 `1`~`4`를 쓰고 하위 번호를 두지 않는다. 계층이 하나뿐이므로 `1-1` 형식이 불필요해졌다.

## 챕터 공통 3단 틀

네 챕터가 모두 같은 3단을 쓴다. 독자가 첫 챕터에서 리듬을 익히면 나머지는 예측하며 읽는다.

| 단 | 내용 | 분량 |
|---|---|---|
| **① 무엇인가** | 짧은 정의 + 흐름 그림 한 장 + 안 하는 것 | 1장 고정 |
| **② 기능별로 깊게** | 기능마다 `무엇 → 어떻게 → 뭐가 깨졌나` | 1~3장 |
| **③ 결과와 한계** | 검증 · 수치 · 못 하는 것 | 1장 고정 |

**챕터 분량은 오직 ②의 개수로만 달라진다.** ①③은 언제나 한 장씩이다.

## 슬라이드 예산

| 구간 | 구성 | 장수 |
|---|---|---|
| 오프닝 · 챕터 지도 | | 3 |
| 1 병원 전화 Voice AI | ①1 + ②3 + ③1 | 5 |
| 2 통화 관측과 평가 | ①1 + ②3 + ③1 | 5 |
| 3 챗봇 고객상담 | ①1 + ②1 + ③1 | 3 |
| 4 AIU 사내 업무지원 | ①1 + ②3 + ③1 | 5 |
| 클로징 | | 1 |
| **합계** | | **22** |

v2의 28장보다 6장 적다. 인/아웃바운드를 합치면서 중복 설명이 사라진 결과이며, 내용이 줄어든 것이 아니다.

## 챕터별 구성

### 1 · 병원 전화 Voice AI (확정)

두 프로젝트의 OpenWiki를 전부 읽고 확정했다.

- 인바운드: `/Users/heewungsong/Desktop/Wise-Ai/Outbound-Agent-Project/inbound-agent/openwiki/` (21문서 954줄)
- 아웃바운드: `/Users/heewungsong/Desktop/Wise-Ai/Outbound-Agent-Project/outbound-agent/openwiki/` (17문서 616줄)

**S1 · ① 이 시스템은 무엇인가**

인바운드 위키 `architecture/overview.md`의 `Core boundaries` 다섯 개를 **뼈대로 쓰되 라벨은 독자 언어로 번역한다.** 위키의 목적은 코드를 안전하게 고치는 것이라 저 이름들은 개발자용 경계다. ①의 목적은 반대이므로 `Configuration boundary` 같은 용어를 그대로 옮기면 다시 시스템 내부 언어가 된다.

| 위키 (개발자용) | ① (독자용) |
|---|---|
| Configuration | 병원마다 다른 규칙을 통화 시작 때 받는다 |
| Conversation | 그 설정대로 대화를 진행한다 |
| Escalation | AI가 처리할지 · 사람에게 넘길지 · 메모로 받을지 정한다 |
| Post-call | 통화가 끝나면 결과를 분석으로 넘긴다 |
| Sensitive-data | 민감한 내용은 밖으로 내보내지 않는다 |

번역하면 앞의 넷은 **통화 한 통의 시간 순서**이고 다섯 번째는 그 전체를 관통하는 규칙이다. 따라서 정적인 경계도가 아니라 **가로 4단계 흐름 + 그것을 받치는 한 줄**로 그린다.

한 장에 들어갈 내용은 다음 다섯이다.

- 정의: 병원의 전화 업무를 AI가 대신한다. 거는 쪽과 받는 쪽 두 방향이며 기능 세트는 공통이다
- 흐름 그림: 위 번역표의 4단계 + 민감정보 규칙
- 기능 다섯: 병원 지식 답변 · 신규 예약 신청 · 예약 조회 · 예약 변경 · 예약 취소, 그리고 상담원 연결(실패 시 메모 접수)
- 규모: 6개 언어(`ko-KR`, `en-US`, `zh-CN`, `ja-JP`, `es-ES`, `vi-VN`), action mode 3종(`auto` / `transfer` / `leave_memo`)
- 안 하는 것: EMR 확정 아님, 병원 근거 없는 답변 안 함(조회 실패 시 no-knowledge 상태로 통화 계속)

`flow_config` 5종 노드는 여기에 흡수한다. 파고들 사건이 없는 구조 설명이므로 ②에 둘 이유가 없다.

기능 다섯 중 **신규 예약만 재설계됐다.** 인바운드 위키가 명시한다. "Inquiry, modification, and cancellation remain legacy tools." 따라서 나머지 넷은 ①에 한 줄씩 놓고 ②에는 넣지 않는다. 원칙을 지키기 위해 억지로 뺀 것이 아니라 실제 구현이 그렇다.

**S2 · ② 신규 예약 — 검색과 동의를 분리하다**

| 겹 | 내용 |
|---|---|
| 무엇 | 통화 중 예약 신청 접수 |
| 어떻게 | `search → stage → commit`. 확인 문구는 오직 `stage_booking`이 생성하며 모델이 만든 시간은 쓰지 않는다 |
| 깨진 것 | "네" 같은 수긍을 검색 조건으로 착각해 날짜를 지어냄 · 소진돼야 할 `EarliestOffer`가 stale 승인으로 재사용 · 빈 검색 결과에 다른 시간을 몰래 대체 |
| 잡는 법 | 예방은 stage-before-commit, 탐지는 `UNSOUND_COMMIT`. 통화에서 한 번도 발화되지 않은 시간이 커밋되면 성공을 강등하며, 끊긴 assistant 발화는 증거로 인정하지 않는다 |

예방과 사후 탐지를 짝으로 만든 것이 이 슬라이드의 핵심이다.

**두 방향의 성숙도 차이를 정직하게 적는다.** 위 내용은 인바운드의 Dynamic Booking v3다. 아웃바운드는 레거시 create 경로가 활성이고(`dynamic_booking_tools`의 import가 주석 처리돼 있다) `create_reservation()`을 호출하지 않은 채 신청 정보를 staging한다. 같은 문제를 두 번 풀었고 두 번째가 더 나았다는 서사로 쓴다. 아웃바운드가 인바운드와 같은 수준으로 구현돼 있다고 서술하면 사실과 다르다.

**S3 · ② 상담원 연결 — 공유 자원의 상태 관리**

두 방향이 **하나의 병원별 FIFO 큐를 실제로 공유**한다. 아웃바운드 위키가 명시한다. "shares a company FIFO queue between inbound/outbound calls."

| 겹 | 내용 |
|---|---|
| 무엇 | 여러 통화가 같은 상담원·trunk를 동시에 요청하는 상황 |
| 어떻게 | `ZSET` 권위 / `HASH` 대시보드 읽기 모델 / `Stream` 알림(PII 없음) 세 구조 분리. 세 키 모두 `{company_id}`를 포함해 같은 Redis Cluster 슬롯에 둔다 |
| 깨진 것 | 재시도가 큐를 새치기하고 좀비 엔트리를 연장 · 늦게 도착한 콜백이 해제된 HASH를 되살림 · 보관된 완료 기록이 활성 통화로 집계 · "오래됐다"만으로 정리하면 아직 갱신 중인 살아있는 통화를 끊음 |

여기에 watchdog 경합을 한 줄 더한다. 전체 무응답 감시 타이머가 DTMF·전환 태스크의 자체 타임아웃과 경합하며, `suppress_user_presence_task`가 그 해법이다.

**S4 · ② 두 방향의 차이 — 시작하는 방법이 다르다**

기능은 같지만 통화가 시작되는 방식이 다르고, 그 차이에서 아웃바운드 고유의 사건이 나온다.

| | 아웃바운드 | 인바운드 |
|---|---|---|
| 시작 | voxBridge가 SIP 참가자를 만들고 **워커는 기다린다** | SIP 수신 후 참가자 admission |
| 게이트 | `FakeAgent` → `sip.callStatus=active`(60초) → **AMD** → human/uncertain일 때만 `SingleAgent` | 유효한 metadata와 참가자 확인 |
| 지식 | FAQ 벌크 프롬프트 주입(`get_faqs`) | Call Context 주입 + no-knowledge fallback |
| 목적 | 발신 시점에 이미 정해져 있다 | 통화 중에 알아내야 한다 |

깨진 것 세 가지를 넣는다.

1. **AMD 판별 중 시작된 발화가 Agent 전환 후 완료되어 설정 인사말 대신 LLM 응답이 나가던 race condition.** 발화가 금지된 `FakeAgent`와 `GreetingDtmfTask`에서 `StopResponse`를 강제해 결정적 인사말 경계를 지켰다.
2. **늦게 도착한 STT 완료가 통화 종료 후 발화처럼 보이던 문제.** 분석 단계에서 `PARTICIPANT_DISCONNECTED`를 끝으로 이동시킨 뒤 분류한다.
3. **일회성 키패드 선택이 이후 자율 도구 호출로 새는 문제.** `get_my_appointment_list`가 시작 시 `from_dtmf_selection`을 먼저 지운다.

동시성 상한도 한 줄 넣는다. `compute_load()`가 활성 잡 5개를 기준으로 계산해 연결 5통 + 셋업 1건까지 허용하고 7번째를 막는다.

**S5 · ③ 결과와 한계**

- 검증: 4단계 **양방향 공통**. 단위 테스트 → LiveKit text-only Agent 평가 → 로컬 환자 시뮬레이션 → 오디오·실제 전화망. 뒤로 갈수록 느리고 현실에 가까우며, 앞 단계에서 걸리는 것을 뒤로 넘기지 않는다는 점을 시각적으로 드러낸다. 위키는 코드 근거 문서라 이 체계를 다루지 않으므로 근거는 `resume/product-engineer.md`다
- 수치: 방향별로 나눠 제시한다. 아웃바운드 550,000+ 통화·일 2,500통·300+ 병원, 인바운드 70,000+ 통화·일 1,500통. 합산 규모를 함께 보여 한 시스템으로 두 방향을 운영했음을 드러낸다
- 한계
  1. 통화 안의 커밋은 예약 *신청* 기록이며 EMR 최종 확정이 아니다
  2. 아웃바운드의 예약 변경·취소는 AI가 실행하지 않는다. 정책에 따라 상담원 연결 또는 메모 접수로 넘긴다
  3. 후처리 전달이 원자적이지 않다. 양쪽 모두 완료 이벤트 발행이 DB 저장보다 먼저이므로 중복 또는 부분 실패가 가능하며 감사 대상이다

한계 3번은 두 위키가 스스로 기록해 둔 내용이다. 그대로 쓰면 자기 시스템의 약한 곳을 아는 사람으로 읽힌다.

### 2 · 통화 관측과 평가 (확정)

챕터의 축은 두 개다. **어떤 과정으로 이루어지는가**(S1~S4)와 **그걸로 무엇을 하는가**(S5). 두 프로젝트를 비교하는 구성은 폐기했다. 대화분석 자체의 기능이 아니라 해석이었기 때문이다.

**S1 · ① 대화분석이란 무엇인가**

- 정의: 통화 수천 건의 결과와 품질을, 사람이 녹취를 다시 듣지 않고 재구성하는 후처리
- 왜 분리돼 있나: 실시간 통화 경로와 분석 부하를 Kafka로 분리했다. 분석이 멈춰도 통화는 계속된다
- 파이프라인 그림 한 장: `입력 → 중복 확인 → 정규화 → 전사 보완 → 요약 → 발행 → 저장 → 결정론적 분석 → 의미 분석 → 점수`
- 안 하는 것: 통화 중에는 아무것도 바꾸지 않는다. 전부 사후 처리다

**S2 · ② 과정 하나 — AI가 못 들은 구간을 복원한다**

| 겹 | 내용 |
|---|---|
| 무엇 | 상담원에게 연결된 뒤로는 AI가 대화에서 빠져 있어 통화 기록이 끊긴다 |
| 어떻게 | 녹음을 CloudFront에서 가져와 전환 성공 이벤트 기준으로 크롭하고, Gemini 구조화 출력으로 발신자·상담원 turn을 복원한 뒤 원본 배열 순서를 보존하며 병합 |
| 깨진 것 | 녹음 앵커가 통화 시작과 어긋나 재생 위치가 밀림 · 너무 짧거나 진폭이 낮은 오디오가 잡음을 만듦 · 녹음이 아직 준비되지 않음(가용성 재시도로 대응) |

`recording_started_at`을 기준 앵커로 삼고 없을 때만 최초 assistant 발화로 폴백한다는 점을 명시한다. 시간 기준점 하나가 전사·재생·분석 전체의 정합성을 좌우한다.

**S3 · ② 과정 둘 — 통화 한 건이 어떻게 끝났는지 판정한다**

| 겹 | 내용 |
|---|---|
| 무엇 | 결과와 실패 책임을 LLM 없이 규칙으로 판정 |
| 어떻게 | `events → attempts → rollup` 3층. 통화당 resolution은 정확히 하나이며 7단계 우선순위가 고정돼 있다. 실패는 단계·유형·책임 주체 3요소로 귀속 |
| 깨진 것 | 공유된 transfer 이벤트가 **이전 모든 intent에 중복 청구**됨 · **시스템 종료를 고객 이탈로 오독** · 없는 `result_count`를 0으로 취급 · 생년월일 입력 후 실패와 입력 자체가 없던 실패를 같게 처리 |

배타적 resolution이 KPI 중복 집계를 막는다는 점을 명시한다. 화면의 "통화 결과 분포"가 이 규칙의 직접적인 산출물이다.

**S4 · ② 과정 셋 — 품질을 평가하되 판정은 건드리지 않는다**

| 겹 | 내용 |
|---|---|
| 무엇 | 임상·긴급도·행정·경험·미지원 요청·응답 품질을 LLM으로 평가 |
| 어떻게 | 모든 소견에 신뢰도와 근거 turn index를 요구하고, sanitizer가 날조되거나 범위를 벗어난 증거를 거부. 결정론 계층과 완전히 분리 |
| 깨진 것 | **assistant 발화가 발신자 의도의 증거로 쓰이던 문제** · 단순 가용성 질문을 예약 의도로 오분류 |

첫 번째가 이 슬라이드의 핵심이다. AI가 "목요일 오후 가능합니다"라고 말한 것을 근거로 환자가 목요일을 원했다고 판정하던 문제이며, 그래서 환자 의도 관련 소견은 user turn만 인용하도록 강제한다.

민감정보 경계는 여기에 한 줄로 붙인다. 의미 소견은 하위 Kafka payload와 Langfuse 전송에서 제외되고, `test_main_kafka_payload.py`가 허용 키 집합을 고정해 재귀 검사하며 `test_semantic_layer_boundary.py`가 결정론 계층의 의미 모듈 의존을 정적으로 차단한다. 규칙이 아니라 실행 가능한 계약이라는 점이 요지다.

**S5 · ③ 이걸로 무엇을 하는가, 그리고 한계**

두 운영 화면이 이 챕터의 결과물이다. 하는 일이 서로 다르므로 나란히 놓는다.

| 화면 | 역할 | 담는 것 |
|---|---|---|
| `assets/cupix-ax-operations-screen.png` 인바운드 운영 현황 | **모니터** — 지표가 어떻게 움직이는가 | 전체 통화·응대 성공률·셀프 처리율·예약 생성 전환율·상담원 연결 성공률, 비교 기간 대비 변화, 그리고 인사말 시작 전·인사말 중·인사 완료 후 무입력으로 나눈 초기 이탈 3분할 |
| `assets/cupix-ax-analytics-screen.png` 인바운드 콜 분석 진단실 | **진단** — 어디서 깨졌고 누가 고치는가 | 통화를 시도 단위로 분해한 손실 지점과 개선 담당 주체, 상호배타 통화 결과 분포, 자체 처리율, 분석 커버리지와 버전 |

운영 도구도 한 줄 넣는다. 버전 기반 백필로 과거 통화를 규칙 변경에 맞춰 다시 계산할 수 있고, 리포트 시점에 신뢰도 임계값을 바꿔도 모델을 다시 돌리지 않는다.

한계 세 가지.

1. 완료 이벤트 발행이 DB 저장보다 먼저다. 중복 또는 부분 실패가 가능하며 감사 대상이다
2. 의미 분석은 결정론적 귀속을 덮어쓸 수 없다. 수동 검토 큐에 신호만 남긴다
3. 상담원 발화가 없으면 상담 결과는 `unknown`으로 남는다. 추정하지 않는다

### 3 · 챗봇 고객상담 (확정)

OpenWiki 19문서 856줄을 읽고 확정했다. 근거는 `/Users/heewungsong/Desktop/Wise-Ai/langgraph-customer-support-agent/openwiki/`.

소유가 부분적이고 분량이 3장이므로 **깊이보다 폭**으로 간다. 이 챗봇이 무엇을 위한 것이고 무엇을 할 수 있는지가 중심이다.

**S1 · ① 이 챗봇은 무엇인가**

- 용도: 병원 웹사이트와 카카오에서 환자 문의를 1차 응대한다
- 역할: 답할 수 있으면 답하고, 필요하면 개인정보를 받고, 안 되면 상담원에게 넘긴다
- 기능 여섯: 병원 지식 답변 · 예약 안내 · 개인정보 수집 · 상담원 연결 신청 · 진료 이미지 제공 · 의료 범위 차단
- 안 하는 것: 예약을 직접 실행하지 않는다(UI를 열어줄 뿐이다), 의학적 판단을 하지 않는다

예약을 실행하지 않는다는 점은 반드시 지킨다. 위키가 명시한다. 예약 도구 목록은 비어 있고 booking API 클라이언트는 LLM 도구로 등록돼 있지 않으며, 대신 `open_reservation_request` 같은 `frontend_action` 메타데이터로 UI를 연다. "챗봇이 예약해준다"고 쓰면 사실과 다르다.

**S2 · ② 상담 한 번이 어떻게 흐르는가**

기능이 실제로 놓인 순서를 그대로 보여준다. LLM 팀을 부르기 전에 결정론적으로 답할 수 있는지 먼저 확인하고, 걸리면 거기서 끝난다.

```
개인정보 미수집?  → 지식 조회를 건너뛰고 수집부터
FAQ 우선          → 등록된 답변을 그대로 (엄격 검증 통과 시)
직접 fact data    → 병원 기준정보에서 즉답
예약 가드         → 요청·조회·변경·취소 의도 분류 후 UI 열기
의료 범위 게이트  → 진단·처방은 여기서 차단
↓ 전부 미해당
LLM 팀 실행
```

하이라이트는 **중간에 끼어드는 절차**다. 환자가 무언가를 묻는 도중 개인정보가 필요해지면 원래 질문을 `pending_question`으로 붙잡아 두고, 수집을 마친 뒤 그 질문으로 돌아온다. 사용자가 같은 말을 두 번 하지 않는다. `collected_info`는 필드 단위로 병합돼 나중 추출이 이미 아는 값을 지우지 않는다.

FAQ 응답은 모델 생성을 건너뛰고 등록된 답변을 그대로 내보내므로, 엄격한 커버리지 검증이 유일한 안전장치라는 점을 한 줄로 적는다.

사건 한 줄: **동의 카드가 중복으로 표시되던 문제.** 이미 활성 개인정보 메시지가 있으면 인사·동의 프롬프트를 다시 넣지 않도록 고쳤다.

**S3 · ③ 결과와 한계**

- 채널 확장을 견딘 기반: 웹으로 시작해 카카오·음성까지 같은 그래프 위에서 동작한다
- 역할은 라벨과 한 줄로만 적는다. "이후 팀이 확장할 수 있는 초기 그래프와 서비스 경계를 구축했다." 항목별로 누가 무엇을 했는지 쪼개지 않는다
- 한계: 예약은 안내까지이며 실행하지 않는다 · 의학적 판단을 하지 않는다 · 대화 저장은 best effort라 실패해도 답변은 계속된다

### 4 · AIU 사내 업무지원 (확정)

OpenWiki 11문서 537줄을 읽고 확정했다. 근거는 `/Users/heewungsong/Desktop/Experiment/OpenBot/openwiki/`.

위키는 `agent-aiu` 서비스와 OpenBot과의 계약만 다루고 OpenBot 플랫폼 전체는 다루지 않는다고 명시한다. 수치와 운영 사실은 `resume/product-engineer.md`를 함께 근거로 쓴다.

**S1 · ① 이 시스템은 무엇인가**

- 용도: 동료가 웹이나 Slack에서 AIU 제품·운영을 물으면 답한다
- 역할: AIU 운영 전문가 코워커. 매뉴얼에서 근거를 찾아 답하고, 저장된 통화를 조회·분석한다
- 기능 여섯: 제품 매뉴얼 답변 · 매뉴얼 스크린샷 제공 · 저장 통화 검색 · 전환율 분석 · 통화 내용 QA · 이미지 생성
- 채널: 웹과 Slack이 하나의 런타임(AG-UI)으로 연결된다

**소유 경계를 그림으로 그린다.** 이 챕터의 정의는 곧 경계다.

```
동료 → OpenBot → AIU        AIU: 추론하고 답한다
                 ↓          OpenBot: 신원 · 권한 · 실행 · 감사
              OpenBot       AIU는 효과를 직접 실행하지 않고 되돌려 보낸다
```

안 하는 것 세 가지를 명시한다. 스스로 실행하지 않는다(권한·정책·감사는 OpenBot이 소유) · 지식에 쓰지 않는다(읽기 전용) · 데이터베이스는 SELECT만 한다.

**S2 · ② 매뉴얼에서 근거를 찾아 답한다**

| 겹 | 내용 |
|---|---|
| 무엇 | Agent Admin · AIU HQ · AIU SVC · 인바운드 · 아웃바운드 다섯 영역의 제품 질문에 답 |
| 어떻게 | **Supervisor는 매뉴얼을 직접 읽지 못한다.** 네 전문 Agent에 위임하고 각자 자기 소스만 읽는다. Deep Agents 파일시스템 권한과 마운트된 백엔드가 호출 신원으로 한 번 더 거르는 이중 방어 |
| 깨진 것 | **접근 거부를 "문서가 없음"으로 답하던 문제.** 목록에서 접근 불가 파일은 숨기지만, 없는 것처럼 단정하면 안 된다 · 매니페스트의 문서 수와 실제 마크다운 수가 어긋나면 시작 시 실패시킨다 |

시작 시 카탈로그가 매니페스트·문서 수·frontmatter 접근 정책·스크린샷 경로를 전부 검증한다는 점을 한 줄로 적는다. 문서 322개를 OKF v0.2 번들로 통합한 규모도 여기에 붙인다.

**S3 · ② 저장된 통화를 조회하고 전환율을 계산한다**

| 겹 | 내용 |
|---|---|
| 무엇 | 인바운드·아웃바운드 통화를 자연어로 검색하고 특정 기간의 예약 전환·자체 처리율을 계산 |
| 어떻게 | SQL은 전부 패키지 소유이고 사용자 값은 바인드 파라미터로만 들어간다. 날짜는 실제 달력 날짜만, 환경은 세 개 enum만 허용. 결과는 최대 50건이며 **이름·전화번호·transcript·녹음·raw payload를 제외**한 운영 메타데이터와 플래그만 담는다 |
| 깨진 것 | **한 전화번호가 여러 행일 때 지표가 부풀려지던 문제** — 정규화된 전화 키로 그룹핑하고 각 플래그에 `MAX`를 써서 한 번호가 각 지표에 최대 1건만 기여하게 했다 · **메모 접수로 끝난 통화를 전환 실패로 세던 문제** — 분모에서 뺀다 · 당일 조회는 최종값이 아니므로 "현재까지"로 서술해야 한다 |

지표 정의를 코드가 소유한다는 것이 요지다. "전환율"이 무엇을 세는지가 질문마다 달라지면 대시보드와 답변이 어긋난다.

**S4 · ② 통화 내용을 검토하되 원문은 내보내지 않는다**

| 겹 | 내용 |
|---|---|
| 무엇 | 특정 통화의 대화 내용을 QA 목적으로 검토 |
| 어떻게 | 정확한 ID 1~5건만 받는다 · 이름과 전화번호를 치환하고 한국 전화·주민등록번호·이메일 패턴을 가린다 · 턴당 1,200자, 긴 대화는 앞뒤 60개로 제한 |
| 경계 | transcript를 **untrusted data로 취급**해 그 안의 지시를 따르지 않는다 · 최종 답변은 반드시 재진술하며 원문과 개인정보를 다시 노출하지 않는다 · 저장된 실행 근거를 보고할 수는 있지만 그것으로 사실을 독립 검증했다고 주장하지 않는다 |

마지막 줄이 이 슬라이드의 핵심이다. 할 수 있는 것과 주장할 수 있는 것을 구분한다.

**S5 · ③ 결과와 한계**

- 규모: 5개 소스 · 문서 322개 · 전문 Agent 4개 · 계약 테스트 45개
- 속도: 2026.08 착수 후 3주 만에 지식 통합·권한 경계·Web/Slack 런타임·자동화 테스트를 갖춘 1차 운영 가능 상태
- 한계
  1. **현재 인증 경계는 단일 full-access 정책이다.** OpenBot이 검증한 run을 신뢰하며, 세밀한 사용자별 role/claim 집행은 완성된 기능으로 주장하지 않는다. 실제 강제 경계는 Supervisor와 전문 Agent 사이의 source isolation이다
  2. 실행 권한을 갖지 않는다. 외부 사이트 접근이나 변경 작업은 OpenBot의 정책·감사·HITL을 거친다
  3. 지식은 읽기 전용이며 런타임에 갱신되지 않는다

한계 1번은 이력서가 이미 그렇게 서술하고 있고 위키의 현재 정책과도 일치한다. 미완성을 완성처럼 쓰지 않은 기록이므로 그대로 살린다.

## 편집 원칙

- 사실은 `resume/product-engineer.md`를 기준으로 하되, 위키에서 더 정확한 사실이 나오면 **먼저 이력서를 고치고** 산출물에 반영한다.
- 위키는 코드에 근거한 문서다. 코드 밖의 실천(Text Eval, Patient Sim 등)은 위키에 없으므로 별도로 확인해 채운다.
- **위키 용어를 ①에 그대로 옮기지 않는다.** 위키는 코드를 안전하게 고치기 위한 문서라 용어가 개발자 기준이다. ①의 독자는 그 반대편에 있다. 구조는 빌리되 라벨은 "무엇을 하는가"의 언어로 번역한다. ②③에서는 `UNSOUND_COMMIT`, `ZSET` 같은 실제 이름을 그대로 써도 된다. 그때는 독자가 이미 시스템을 알고 있고, 구체적인 이름이 오히려 신뢰를 만든다.
- 측정하지 않은 절감 시간, 전사 확산률 같은 성과는 주장하지 않는다.
- 특정 회사명과 지원 직무 문구는 포함하지 않는다.
- 도메인 밖 용어는 최초 등장 시 3~4단어로 각주를 단다. 최소한 `DTMF`(전화 키패드 입력), `trunk`(대표번호 회선)는 필요하다.
- ②의 "뭐가 깨졌나"는 각색하지 않고 위키 기록을 근거로 쓴다. 근거가 없으면 그 겹을 비우지 말고 기능 자체를 ②에서 뺀다.
- **②의 기능은 "무엇을 하는가"로 고르지, "어디에 좋은 사건이 있나"로 고르지 않는다.** 흥미로운 버그가 있다는 이유로 사용자에게 의미 없는 내부 구조를 ②에 올리면 챕터가 엔지니어링 일지가 된다. 먼저 이 시스템이 사용자를 위해 무엇을 하는지 나열하고, 그중에서 사건이 붙는 것을 고른다. 순서를 뒤집지 않는다.
- **소유 범위는 역할 라벨과 한 줄로만 적는다.** 어느 기능을 누가 만들었는지 항목별로 쪼개지 않는다. 검증할 수 없고 방어적으로 읽힌다. 팀과 함께한 프로젝트는 "이후 팀이 확장할 수 있는 기반을 만들었다" 정도의 높이를 유지한다.

## 시각 방향

v2의 슬라이드 시스템(16:9, rem 기반 스케일, 다크/라이트)을 유지한다. 구조 변화에 맞춰 두 가지를 더한다.

- **①의 흐름 그림**을 챕터마다 같은 시각 문법으로 그린다. 네 챕터의 ①을 나란히 놓았을 때 같은 종류의 그림으로 보여야 한다.
- **②의 세 겹**을 고정된 시각 패턴으로 표현해, 독자가 세 번째 블록이 항상 실패 사례임을 학습하게 한다.

## 미결 사항

없다. 네 챕터 모두 각 프로젝트 OpenWiki를 읽고 확정했다.

### 해결됨

- **챗봇 고객상담의 소유 범위를 어디까지 적을 것인가** → 항목별로 적지 않는다. 역할 라벨과 한 줄로만 두며, 어느 채널·기능을 누가 만들었는지는 확정할 수 없고 포트폴리오에 필요하지도 않다. 편집 원칙에 규칙으로 올렸다.

- **AIU의 ② 기능 선별** → 매뉴얼 답변 · 통화 조회와 전환율 · 통화 QA 셋으로 확정했다. 실행 권한을 갖지 않는다는 소유 경계는 ②가 아니라 ①의 정의와 ③의 한계에 둔다.
- **아웃바운드가 인바운드의 분석 파이프라인을 공유하는가** → 별도다. 토픽·테이블·판정 방식이 모두 다르며 성숙도가 한 세대 차이 난다. 이 사실이 관측 챕터의 중심 서사가 됐다. 정보 구조 절 참조.
- **검증 4단계가 아웃바운드에도 적용되는가** → 양방향 공통이다. 챕터 1의 ③에 그대로 넣는다. 단계 명칭의 근거는 `resume/product-engineer.md`이며 위키에는 없다.
- **관측 챕터를 두 파이프라인 비교로 쓸 것인가** → 쓰지 않는다. 비교는 대화분석의 기능이 아니라 해석이었다. 챕터 2는 과정과 용도 두 축으로 전개한다.
- **아웃바운드 신규 예약 경로의 현재 상태** → 레거시 `ScheduleSelectionTask` 경로가 맞다. 위키 기술이 정확하다. 이에 따라 `resume/product-engineer.md`의 아웃바운드 신규 예약 서술에서 인바운드 개념인 "후보 스테이징과 최종 동의" 표현을 제거하고, 실제 동작인 재선택 3회 허용과 스냅샷 교체 기록으로 대체했다(2026-08-25). 챕터 1의 ② 신규 예약은 "같은 문제를 두 번 풀었고 두 번째가 더 나았다"는 서사로 쓴다.

## 완료 기준

- 도메인을 모르는 독자가 각 챕터의 ① 한 장만 보고 그 시스템이 무엇인지 말할 수 있다.
- ②의 모든 슬라이드에 실패 사례 또는 의도적으로 그은 경계가 근거와 함께 들어가 있다. 각색이나 추측이 없다.
- AIU 챕터를 읽고 나면 이 시스템이 무엇을 하고 **무엇을 하지 않기로 했는지** 둘 다 말할 수 있다.
- 인바운드와 아웃바운드가 한 시스템의 두 방향으로 읽히며, 어느 한쪽의 규모나 판단이 묻히지 않는다.
- 관측과 평가 챕터를 읽고 나면 대화분석이 어떤 과정으로 이루어지고 그 결과를 운영자가 어떻게 쓰는지 둘 다 말할 수 있다.
- 모든 챕터가 마지막에 못 하는 것을 명시한다.
- 네 챕터가 같은 3단 리듬을 유지하며, 분량 차이가 ②의 개수에서만 발생한다.
- 키보드·터치 탐색, 다크/라이트 테마, 16:9 및 모바일 뷰포트에서 정상 표시된다.

## 후속 정리

구조가 확정되면 `README.md`, `docs/workflow.md`, 관련 문서 내부 링크를 함께 맞춘다. v2를 남길지 대체할지는 v3 완성 시점에 결정한다.
