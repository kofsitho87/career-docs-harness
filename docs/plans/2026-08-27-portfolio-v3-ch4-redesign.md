# 포트폴리오 v3 챕터 4 · 6장 재설계 작업 기록

## 메타

- 작성일: 2026-08-27
- 대상 산출물: `portfolio/heewung-song-portfolio-v3.html`
- 대상 범위: CH 04 AIU 사내 업무지원 (슬라이드 24~29, 덱 30장 중)
- 목적: 기존 5장을 "운영 동료의 하루" 시나리오로 재구성해, 프로젝트를 모르는 채용담당자도 각 장을 직관적으로 이해하게 한다.
- 선행 설계: `docs/plans/2026-08-25-portfolio-v3-structure-design.md`
- 사실 기준: `resume/product-engineer.md` (사내 AIU 업무지원 Multi-Agent 및 AX 자동화 플랫폼 구축, 2026.08 - 현재)
- 기술 근거: `/Users/heewungsong/Desktop/Experiment/OpenBot/openwiki/` (overview, specialists-and-knowledge, analytics, openbot-integration, tools-and-attachments)

## 사용자 인터뷰 결과 (2026-08-27)

- 핵심 인상: **실용성 — 동료의 시간을 아낀다**. 경계 설계·속도·패턴 반복보다 이 인상을 전면에 세운다.
- 시나리오 구성: **하루 업무 흐름**. 운영 동료의 하루를 따라가며 기능이 등장한다.
- 운영 증거: 실제 사용 화면을 쓴다. 사용량 수치는 없으므로 **구축 규모 수치만** 사용한다(5개 소스 · 322개 문서 · 4개 전문 Agent · 45개 테스트 · 3주).
- 장수: 5장 유지(A안). 다만 **5장(마무리)은 "3주 구축 성과" 대신 "확장 비전 — AX 자동화"로 변경**한다.
- 추가 피드백(2026-08-27, 구현 후): ① 1장 제목을 **"사내 AX 프로젝트"라는 대주제**로 재설정한다. ② **시스템 아키텍처 + 기술 스택 장을 신설**해 CH 04를 6장으로 확장한다(덱 30장). ③ 통화 검토 장 제목을 "문제 통화를, 개인정보 없이 검토합니다"에서 "무슨 일이 있었는지, 안전장치 안에서 확인합니다"로 교체한다.

## 작업 상태 정의

각 장은 아래 네 단계를 모두 만족해야 완료로 본다.

1. **내용 확정**: 헤드라인, 핵심 메시지, 포함·제외 범위를 사용자와 합의했다.
2. **구현**: 대상 HTML과 필요한 CSS를 수정했다.
3. **정적 검증**: JavaScript parse, 필수 문구와 HTML 구조 균형 검사를 통과했다.
4. **화면 확인**: 실제 브라우저에서 16:9 화면을 확인하고 필요 시 사용자 피드백을 반영했다.

## 확정된 편집 원칙

- 슬라이드 물리 순서는 최종적으로 `ch4-what → ch4-arch → ch4-analytics → ch4-qa → ch4-manual → ch4-result`다(1차 재배열 후 3차에서 `ch4-arch` 신설).
- 2~4장은 한 사람(운영 동료)의 하루로 연결한다: **09:30 어제 성과 확인 → 10:00 문제 통화 검토 → 14:00 병원 문의 응대**. 1장에서 이 타임라인을 미리 보여준다.
- 2장(통화 조회)과 3장(QA)은 실제 제품 흐름으로 인과 연결한다: 전환 지표 답변에서 실패 통화를 확인하고, 그 통화 ID로 비식별 검토를 이어간다.
- 각 기능 장의 공통 구조: **동료의 실제 질문(말풍선) → AIU가 답을 만드는 과정(간단 도식) → 지켜지는 경계 한 줄**. 경계(SELECT-only, source isolation, 마스킹)는 장당 한 줄로 압축해 실용성이 주인공이 되게 한다.
- AG-UI, Deep Agents, OKF 같은 내부 스택 이름은 본문 문장에서 빼고, 필요한 곳에만 모노스페이스 캡션으로 남긴다. AIU·OpenBot은 1장에서 한 문장으로 정의한 뒤 사용한다.
- 매뉴얼 장은 실제 답변 화면(`assets/aiu-web-knowledge-answer.png`)을 증거로 유지한다.
- 사용량 수치를 만들지 않는다. 규모 수치는 사실 기준 문서에 있는 것만 쓴다: 5개 소스, 322개 문서, 4개 전문 Agent, 45개 unit/contract test, 2026.08 착수 후 3주.
- 5장(비전)은 현재 사실과 방향을 구분한다. **이미 있는 것**(OpenBot의 정책·감사·HITL·컨테이너 브라우저 실행 기반, AIU의 읽기 전용 경계)은 사실로, **확장(실행 업무 자동화)**은 진행 방향으로 명시하며 완성된 기능으로 주장하지 않는다.
- 챕터 지도의 CH 04는 6장으로 표기한다. `showSlide()` 챕터 시작 인덱스(4·11·19·24)는 CH 04가 마지막 챕터라 변경이 없고, 총 장수는 JS가 동적으로 계산한다. 정적 표기(지도 "6 slides", rail "28 slides", 초기 카운터 "1 / 30")만 갱신한다.

## 최종 6장 구성과 진행 상태

| 장 | 헤드라인 | 대상 섹션 | 시나리오 | 내용 확정 | 구현 | 정적 검증 | 화면 확인 |
|---:|---|---|---|:---:|:---:|:---:|:---:|
| 1 | 사내 AX 프로젝트 — 동료의 반복 업무를 AI 코워커에게 | `ch4-what` | 대주제 + 하루 타임라인 예고 | [x] | [x] | [x] | [x] |
| 2 | OpenBot 위에서, Supervisor가 네 전문 Agent를 지휘합니다 | `ch4-arch` | 시스템 아키텍처 + 기술 스택 | [x] | [x] | [x] | [x] |
| 3 | 자연어로 물으면, 검증된 지표로 답합니다 | `ch4-analytics` | 09:30 어제 성과 확인 | [x] | [x] | [x] | [x] |
| 4 | 무슨 일이 있었는지, 안전장치 안에서 확인합니다 | `ch4-qa` | 10:00 문제 통화 검토 | [x] | [x] | [x] | [x] |
| 5 | 322개 매뉴얼에서 근거를 찾아, 화면까지 붙여 답합니다 | `ch4-manual` | 14:00 병원 문의 응대 | [x] | [x] | [x] | [x] |
| 6 | 다음 단계 — 찾아주는 AI에서, 실행하는 AI로 | `ch4-result` | 마무리 · AX 자동화 비전 | [x] | [x] | [x] | [x] |

상태 해석:

- 내용 확정은 2026-08-27 사용자 인터뷰와 A안(+5장 주제 변경) 승인으로 완료했다.
- 헤드라인 문구는 구현 중 다듬을 수 있으나 각 장의 주제와 시나리오는 합의된 대로 유지한다.

## 장별 완료 체크리스트

### 1장 · 소개 (`ch4-what`)

- [x] 3차: 제목을 "사내 AX 프로젝트" 대주제로 재설정하고 AX 정의를 support에 추가

- [x] 첫 문장에서 프로젝트를 한 줄로 정의: 동료가 여러 사내 시스템을 뒤지던 질문을 Slack·웹에서 한 번에 답하는 사내 AI 코워커
- [x] AIU(병원 AI 제품군 이름)와 OpenBot(신원·권한·실행·감사 담당 기반)을 각각 한 문장으로 정의
- [x] 하루 타임라인 미리보기(09:30 성과 확인 · 10:00 통화 검토 · 14:00 문의 응대) 배치
- [x] 기간(2026.08 – 현재)·역할(설계·구축) 표시
- [x] "안 하는 것"(실행하지 않음·지식 읽기 전용·SELECT만)은 압축해 유지하되 분량을 줄임
- [x] 기능 여섯 chips 나열은 타임라인 중심 구성으로 대체
- [x] 정적 검증
- [x] 사용자 화면 확인

### 2장 · 구조와 기술 (`ch4-arch`) — 3차 신설

- [x] 동료 → OpenBot 플랫폼 → AIU Supervisor 흐름도
- [x] 범위를 나눈 전문 Agent 넷 카드
- [x] 지식 번들(5개 제품·322개 문서·읽기 전용)과 운영 DB(SELECT-only) 카드
- [x] 기술 스택 chips(TypeScript·Bun·Deep Agents·LangGraph·AG-UI·GPT-5.6 Luna·Gemini·MySQL·Zod·Slack·Docker)
- [x] 정적 검증
- [x] 사용자 화면 확인

### 3장 · 09:30 어제 성과 확인 (`ch4-analytics`)

- [x] 동료의 실제 질문 말풍선으로 시작: 어제 특정 병원의 예약 전환 결과 질의
- [x] 자연어 질문 → 검증된 SQL 지표 → 답변의 과정 도식
- [x] "지표 정의는 코드가 소유한다"(질문마다 전환율 정의가 달라지지 않음) 메시지 유지
- [x] 한 전화번호 중복 집계 방지·메모 접수 분모 제외 같은 지표 정확성 사례는 1~2개로 압축
- [x] 경계 한 줄: SELECT-only · 이름/전화번호/녹음 원문 제외
- [x] 다음 장(문제 통화 검토)으로 이어지는 연결 문구 배치
- [x] 정적 검증
- [x] 사용자 화면 확인

### 4장 · 10:00 문제 통화 검토 (`ch4-qa`)

- [x] 앞 장에서 이어지는 질문 말풍선: 실패 통화 하나를 지정해 검토 요청
- [x] 명시한 통화만(1~5건) → 개인정보 마스킹 → 검토 답변의 과정 도식
- [x] 4단계 gate(ID 범위·마스킹·턴 길이·긴 대화)는 유지하되 표현을 하루 시나리오에 맞게 조정
- [x] 경계 한 줄: 원문·개인정보는 최종 답변에 재노출하지 않음, transcript 내 지시를 따르지 않음
- [x] 정적 검증
- [x] 사용자 화면 확인

### 5장 · 14:00 병원 문의 응대 (`ch4-manual`)

- [x] 시나리오 말풍선: 병원에서 온 화면 사용법 문의
- [x] 질문 → 담당 전문 Agent 위임 → 매뉴얼 근거 + 스크린샷 답변의 과정 도식
- [x] 4개 전문 Agent가 각자 자기 소스만 읽는 구조(source isolation)를 쉬운 문장으로 유지
- [x] 322개 문서·5개 소스 수치 표시
- [x] 실제 답변 화면(`assets/aiu-web-knowledge-answer.png`) 유지
- [x] "접근 거부를 문서 없음으로 답하지 않는다" 사례는 유지 여부를 구현 중 판단(밀도 초과 시 제거)
- [x] 정적 검증
- [x] 사용자 화면 확인

### 6장 · 확장 비전 — AX 자동화 (`ch4-result`)

- [x] 현재 상태를 한 줄로 정리: 지금은 찾아서 답하는 읽기 전용 코워커
- [x] 이미 갖춰진 실행 기반을 사실로 표시: OpenBot이 소유한 정책 평가 → 감사 기록 → 실행 게이트웨이, 컨테이너 브라우저(Computer Use), HITL·권한 부여
- [x] 확장 방향 표시: 매뉴얼로 안내하던 반복 운영 업무를, 같은 거버넌스 위에서 AI가 직접 수행하는 AX 자동화로 확장
- [x] 완성 주장 금지: 실행 자동화는 진행 방향이며 현재 AIU는 실행 권한을 갖지 않음을 명시
- [x] 규모 수치(5개 소스·322개 문서·4개 전문 Agent·45개 테스트·3주 구축)를 비전의 토대로 압축 배치
- [x] 기존 한계 3개 나열은 제거하고, 핵심 경계(실행 권한은 OpenBot 소유) 한 줄만 유지
- [x] 정적 검증
- [x] 사용자 화면 확인

## 기존 5장과 새 6장의 매핑

| 기존 섹션(순서) | 처리 계획 | 새 순서 |
|---|---|---:|
| `ch4-what` (1) | 하루 타임라인 중심 소개로 전면 수정 | 1 |
| `ch4-manual` (2) | 14:00 병원 문의 시나리오로 수정 후 5번째로 이동 | 5 |
| `ch4-analytics` (3) | 09:30 성과 확인 시나리오로 수정 후 3번째로 이동 | 3 |
| `ch4-qa` (4) | 10:00 문제 통화 검토 시나리오로 수정 후 4번째로 이동 | 4 |
| `ch4-result` (5) | 3주 구축 성과에서 AX 자동화 비전으로 주제 변경 | 6 |
| (신설) | 구조와 기술(`ch4-arch`) 장을 3차에서 추가 | 2 |

## 전체 완료 전 정리 체크리스트

- [x] HTML 섹션 물리 순서 재배열 후 CH 04가 정확히 6장인지 확인
- [x] 슬라이드 순서 변경이 rail-track·주석 번호와 어긋나지 않는지 확인
- [x] 챕터 지도의 CH 04 문구("3주 MVP · 검증 중", 한 줄 설명)가 새 구성과 맞는지 확인하고 필요 시 조정
- [x] 다크·라이트 테마 확인
- [x] 1920×1080 화면 확인
- [x] `README.md`·`docs/workflow.md`·`2026-08-25-portfolio-v3-structure-design.md`의 장수 표기를 30장·CH 04 6장으로 갱신

## 근거 자료

- `resume/product-engineer.md` — 사내 AIU 업무지원 Multi-Agent 및 AX 자동화 플랫폼 구축 항목
- `/Users/heewungsong/Desktop/Experiment/OpenBot/openwiki/overview.md` — Supervisor·4 전문 Agent 구조, 소유권 경계
- `/Users/heewungsong/Desktop/Experiment/OpenBot/openwiki/specialists-and-knowledge.md` — source isolation, 322개 문서 검증, 읽기 전용
- `/Users/heewungsong/Desktop/Experiment/OpenBot/openwiki/analytics.md` — SELECT-only 조회, 전환 지표 정의, 비식별 통화 검토
- `/Users/heewungsong/Desktop/Experiment/OpenBot/openwiki/openbot-integration.md` — 서명된 run·콜백 토큰, OpenBot 거버넌스 경계
- `/Users/heewungsong/Desktop/Experiment/OpenBot/openwiki/tools-and-attachments.md` — 도구 소유권 분리, 서명 URL attachment
- `/Users/heewungsong/Desktop/Experiment/OpenBot/README.md` — 게이트웨이(정책 평가→감사 기록→실행), 컨테이너 브라우저, `/admin/audit`
- `assets/aiu-web-knowledge-answer.png` — 실제 매뉴얼 답변 화면

## 작업 로그

### 2026-08-27 · 1차

- 채용담당자 관점 진단: 용어 장벽, 시나리오 부재, 경계 설명 과다, 결과 장 인상 약함을 확인했다.
- 사용자 인터뷰로 핵심 인상(실용성), 시나리오(하루 업무 흐름), 증거(실제 화면 + 구축 규모 수치만), 장수(조정 가능)를 확정했다.
- A안(5장 유지, 하루 흐름 재배열)을 승인받고, 5장 주제를 "3주 구축 성과"에서 "확장 비전 — AX 자동화"로 변경하기로 합의했다.
- OpenBot 저장소의 openwiki 페이지 6개와 README로 기술 근거를 수집하고 이 설계 문서를 작성했다.

### 2026-08-27 · 2차

- CH4 전용 CSS(`dayline`·`day-step`·`ask`·`ask-time`·`ask-bubble`·`vis-board`·`vis-col`)와 좁은 화면 responsive 규칙을 추가했다.
- 1장 `ch4-what`을 하루 타임라인 미리보기 중심 소개로 전면 수정하고, 기능 여섯 chips와 "안 하는 것" 목록을 각각 타임라인과 note 한 줄로 대체했다.
- HTML 섹션을 `what → analytics → qa → manual → result` 순서로 재배열했다. 2~4장은 시간 배지가 붙은 동료 질문 말풍선으로 시작한다.
- 2장 `ch4-analytics`에 질문 → 코드가 소유한 조회 → 지표 답변 도식과 지표 정확성 사례 2개, 다음 장 연결 문구를 배치했다.
- 3장 `ch4-qa`는 2장의 답변에서 이어지는 통화 ID 지정 시나리오로 바꾸고, 4단계 안전장치 gate를 쉬운 표현으로 조정했다.
- 4장 `ch4-manual`은 전문 Agent 이름 코드(`inbound-agent` 등)를 "받는 전화 담당" 같은 역할 이름으로 바꾸고, 실제 답변 화면을 유지했다.
- 5장 `ch4-result`를 NOW(운영 중 · 규모 수치) → NEXT(같은 거버넌스 위 AX 자동화) 비전 구성으로 교체하고, 실행 자동화는 진행 방향임을 명시했다.
- 낡은 CH3 5장 주석("결과와 한계")을 "운영 결과와 배운 점"으로 정정했다.
- 정적 검증 통과: JavaScript parse, 13개 태그 구조 균형, CH4 섹션 순서, 필수 문구 존재, 이전 구성 문구 제거, trailing whitespace 없음.
- 다크 테마 1920×1080에서 5장 전부 렌더링을 확인했다(콘텐츠 높이 583~793px, 가로 오버플로 없음). 챕터 지도의 CH 04 문구는 새 구성과 어긋나지 않아 유지했다.
- 라이트 테마에서 1장 렌더링과 좁은 화면 responsive 세로 배치를 확인했다.
- (3차에서 이어짐)

### 2026-08-27 · 3차 (사용자 피드백 반영)

- 통화 검토 장 제목을 "문제 통화를, 개인정보 없이 검토합니다"에서 **"무슨 일이 있었는지, 안전장치 안에서 확인합니다"**로 교체했다.
- 1장 제목을 **"사내 AX 프로젝트 — 동료의 반복 업무를 AI 코워커에게"**로 재설정하고, support에 AX(AI Transformation) 정의를 추가했다.
- `ch4-arch`(구조와 기술) 장을 신설해 1장 뒤에 배치했다: 동료 → OpenBot 플랫폼 → AIU Supervisor 흐름, 전문 Agent 넷, 지식 번들(읽기 전용)·운영 DB(SELECT-only), 기술 스택 chips(TypeScript·Bun·Deep Agents·LangGraph·AG-UI·GPT-5.6 Luna·Gemini·MySQL·Zod·Slack·Docker). 수치·스택은 `resume/product-engineer.md`의 해당 항목만 사용했다.
- CH 04는 6장, 덱 전체 30장이 됐다. 챕터 지도 "5 slides"→"6 slides", 지도 rail "27 slides"→"28 slides", 초기 카운터 "1 / 29"→"1 / 30", CH4·CLOSING 주석 번호를 갱신했다. `showSlide()` 이동 인덱스(4·11·19·24)는 CH4가 마지막 챕터라 변경이 없고, 총 장수는 JS가 동적으로 계산한다.
- 정적 검증 재통과(JS parse, 태그 균형, 30개 섹션, CH4 순서 `what → arch → analytics → qa → manual → result`), 1920×1080에서 신설 장(766px)과 수정된 1장(698px) 렌더링을 확인했다.
- 사용자가 24~29페이지 화면을 확인했다. CH 04 여섯 장 모두 네 단계(내용 확정·구현·정적 검증·화면 확인)를 완료했다.

### 2026-08-27 · 4차 (클로징 장)

- 클로징(30페이지)을 "한 문장 선언" 구성으로 전면 수정했다: 3행 선언 헤드라인 유지, 4단계 방법론 카드와 "안 하는 것" 문단 제거, 챕터 색 상단 보더의 대형 지표 4개(누적 AI 통화 620,000+ · 4,000건/일 · 도입 병원 300+ · 프로덕션 AI 제품 4개) 배치, 연락처 유지.
- 지표는 사실 기준 문서와 CH 01 성과 장의 기존 수치만 사용했다. `close-loop`/`close-step` CSS를 `close-stats`/`close-stat`로 교체하고 responsive 규칙을 갱신했다.
- 정적 검증 통과, 1920×1080 렌더링 확인(콘텐츠 737px, 오버플로 없음).
- 사용자 피드백을 반영해 헤드라인을 정체성 문구 **"반복되는 현장 업무를, 멈추지 않고 일하는 AI 제품으로 바꿉니다."**로 확정하고, 기존 방법론 선언("업무를 이해하고, 할 일과 안 할 일을 정하고, 운영으로 증명합니다")은 부제로 이동했다.

### 2026-08-27 · 5차 (클로징 장 — 1페이지 중복 해소)

- 사용자가 1페이지와 30페이지의 중복을 지적했다: 헤드라인 문장 구조("반복되는 ~ 업무를, ~ AI ~로")가 사실상 같고, 하단 지표 4개 중 3개(620,000+ · 4,000건/일 · 300+)가 히어로 스탯과 겹쳤다.
- 역할을 분리했다: 1페이지는 "선언"(무엇을 하는 사람인가), 30페이지는 "닫는 말"(28장을 본 사람에게 남기는 마지막 메시지).
- 헤드라인을 사용자가 제안한 문장 기반으로 교체했다: **"AI를 가능성에서 / 프로덕션으로 옮기는 / 엔지니어입니다."** (제시한 변형 A·B·C 중 원문 유지형 C를 사용자가 선택). 1페이지와 단어가 겹치지 않으면서 "데모가 아니라 운영"이라는 덱의 차별점을 정체성으로 정리한다.
- 하단 지표 4개를 챕터별 고유 수치로 전면 교체해 히어로 스탯과의 중복을 제거했다: **91.0%** 예약 조회 요청 완료율(CH 01 · VOICE AI), **28개** 통화마다 감지하는 활동 플래그(CH 02 · CALL ANALYTICS), **7,464명** 누적 상담 시작 사용자(CH 03 · CHATBOT), **322개** AI가 근거로 답하는 매뉴얼(CH 04 · AIU). 네 수치 모두 각 챕터 본문 장에 이미 있는 수치만 재사용했고, 기존 챕터 색 상단 보더 순서(CH1→CH4)와 캡션이 일치한다.
- 정적 검증 통과(JS parse, 태그 균형, 신규 문구 존재·이전 문구 제거), 1920×1080 렌더링 확인. 사용자가 화면을 확인하면 클로징 장 네 단계가 완료된다.
